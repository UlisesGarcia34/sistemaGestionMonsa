/// <reference types="vite/client" />

// Habilita los tipos de los imports que resuelve Vite y no TypeScript por su
// cuenta: en este proyecto, `import logo from "@/assets/LogoMonsa.png"` (que
// devuelve la URL final del asset, con hash, tras el build).
